# Flask-app-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: flask-app
  namespace: capstone
spec:
  type: NodePort
  selector:
    app: flask-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080